experiment_name: rfdetr_large_split_v1_expanded_v2
model: rfdetr
size: large
split: split_v1_baseline_expanded
imgsz: 512
epochs: 100
batch_size: 4
device: "cuda:0"
freeze_backbone: false
grad_accum_steps: 4
augmentations: a2_offline_expanded
notes: "RF-DETR Large — offline expanded dataset (6x train), thermal sim disabled online"
