experiment_name: rfdetr_base_split_v1_baseline
model: rfdetr
size: base
split: split_v1_baseline
imgsz: 512
epochs: 100
batch_size: 4
device: "cuda:0"
freeze_backbone: false
grad_accum_steps: 4
notes: "RF-DETR Base ~29M — cross-architecture comparable (Large tier)"
augmentations: a1_baseline
