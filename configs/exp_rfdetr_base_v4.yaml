experiment_name: rfdetr_base_split_v1_v4_merged
model: rfdetr
size: base
split: split_v1_v4_merged
imgsz: 512
epochs: 100
batch_size: 4
device: "cuda"
freeze_backbone: false
grad_accum_steps: 4
augmentations: a2_offline_expanded
resume: auto
notes: "RF-DETR Base — v4: merged domain classes, 6 classes"
